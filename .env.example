# Copy this file to .env (which is already git-ignored) and fill in real values.
# Never commit the actual .env file.

# Used to sign appointment reference numbers (staff/check-in.php).
# Generate one with: php -r "echo bin2hex(random_bytes(32));"
REFERENCE_NUMBER_SECRET=
